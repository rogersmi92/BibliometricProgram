Map reference data lives under this folder.

Natural Earth is no longer required. The pipeline uses local Census, geoBoundaries, GeoNames, custom gazetteer, and institution reference folders when available.
